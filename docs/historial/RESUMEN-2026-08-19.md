# Resumen de avances — 19 de agosto de 2026

## ✅ Completado hoy

### 1. Dashboard — Filtros buscables
- **Material en vales**: se agregó un campo de búsqueda por código/descripción que filtra vía backend (`/api/almacen/vales?busqueda=...`).
- **Historial de ventas**: los filtros de cliente y código ahora usan `SearchableSelect` (igual que el cotizador).
- Se copió el componente `SearchableSelect` a `DashboardPage.jsx`.
- Archivos: `src/pages/DashboardPage.jsx`

### 2. Cotizador — Stock en León numérico
- El botón "Con stock en León / Sin stock en León" volvió a aparecer (igual que el de descuento) para mostrar/ocultar la columna.
- La columna **Stock León** ahora es un input numérico (antes era un check).
- Se ajustó el backend para recibir `stock_leon` como entero.
- Archivos: `src/pages/CotizadorPage.jsx`, `api/app/cotizaciones/router.py`

### 3. Cotizador — Dropdowns dentro de la tabla
- Se corrigió el `SearchableSelect` para que la lista de opciones se renderice con `position: fixed` y alto `z-index`. Esto evita que quede oculta dentro de la tabla con scroll horizontal.
- Se aplicó el mismo cambio en `DashboardPage.jsx`.
- Archivos: `src/pages/CotizadorPage.jsx`, `src/pages/DashboardPage.jsx`

### 4. Cotizador — PDF con formato corporativo de 3P
- Se reprodujo el diseño del Excel `Y:\COTIZACIONES\1. COTIZADOR\2. COTIZADOR 2.0.xlsm`.
- Se generó un logo horizontal de alta resolución a partir de `images/LOGO 3P.png`.
- Se extrajeron y agregaron las firmas de los vendedores:
  - America Ruiz
  - Carlos Urbina
  - Cynthia Hernandez
- El PDF ahora incluye:
  - Logo en encabezado con recuadro.
  - Título "FORMATO / C O T I Z A C I Ó N".
  - Folio en recuadro gris + fecha larga en español.
  - Cliente y Atención a en recuadros gris separados.
  - Texto "Presente".
  - Tabla de productos con bordes gruesos.
  - Totales (Subtotal, IVA 16%, Total).
  - Notas de condiciones.
  - Firma según el vendedor seleccionado.
  - Pie de página con dirección, teléfonos y correo.
- Archivos: `api/app/cotizaciones/router.py`, `api/app/cotizaciones/assets/*`

## 🚀 Despliegue
- `npm run build` ✅
- `npm run deploy` ✅ (GitHub Pages)
- Backend reiniciado con `api/tools/restart-backend.ps1` ✅
- Commit y push a `master` ✅

## 📁 Archivos modificados/agregados hoy
```
src/pages/DashboardPage.jsx
src/pages/CotizadorPage.jsx
api/app/cotizaciones/router.py
api/app/cotizaciones/assets/logo.png
api/app/cotizaciones/assets/firma_america_ruiz.png
api/app/cotizaciones/assets/firma_carlos_urbina.png
api/app/cotizaciones/assets/firma_cynthia_hernandez.png
docs/RESUMEN-2026-08-19.md
```

## 📋 Pendientes para seguir mañana
1. **Programar aplicaciones al inicio de Windows**
   - Outlook, WhatsApp escritorio, Microsoft Edge con Gmail, SAE.
   - iVMS-4200 3.12.1.5 Client solo los lunes.
   - Archivos relevantes: `api/tools/start-morning-apps.ps1`, `api/tools/register-autostart.ps1`.

2. **Sección Valor de inventario**
   - Revisar si el snapshot se guarda automáticamente o requiere botón.
   - Cambiar la gráfica a línea para ver tendencia.
   - Archivo: `src/pages/DashboardPage.jsx` (pestaña `valor-inventario`).

3. **Errores 404 en consola del dashboard/existencias**
   - `GET https://api.3psadecv.com/api/almacen/foto-producto/... 404`
   - Error del listener asíncrono.
   - Archivos: `src/pages/DashboardPage.jsx`, backend `api/app/almacen/router.py`.

4. **Subir fotos de productos desde la web**
   - Guardarlas en la misma ruta que usa el Excel/formulario.
   - Pendiente de definir ruta y flujo.

5. **Cotizador — Error 404 al descargar PDF**
   - Verificar endpoint `/api/cotizaciones/{id}/pdf` en producción.
   - Puede estar relacionado con CORS o con la ruta del asset del logo.

## 📝 Notas técnicas
- El logo del PDF se genera dinámicamente con PIL a partir del logo principal del proyecto.
- Las firmas se seleccionan por nombre del vendedor (case-insensitive).
- El backend de cotizaciones usa ReportLab; las imágenes deben estar en `api/app/cotizaciones/assets/`.
- La base de datos local de pruebas quedó en `data/cotizaciones.db` (no se subió al repo).
