# Cierre de sesión — 2026-09-08

## Tema de la sesión: catálogo MS Schippers, mejoras al sitio público y revisión del panel (Pedidos abiertos + cliente en historial)

Sesión de cierre de la dinámica marca-por-marca del catálogo público y arranque de
la revisión del panel de administración. Todo lo del sitio público ya está desplegado
en 3psadecv.com.

---

## 1. Lo logrado hoy

### 1.1 Catálogo MS Schippers — fotos y productos (commits `d9e45aa`, `0114468`, `47446aa`)

- **Auditoría:** 45/45 imágenes referenciadas existen en disco (sin rotas ni
  huérfanas); las fotos de `CATALOGO AUTORIZADO/MS DETERGENTES/` ya estaban todas
  asignadas.
- **6 productos sin foto** → 2 se resolvieron con foto de variante a petición del
  usuario: `Q5` (1 L) usa `q5-20.png` (foto del de 20 L, **pendiente de revisión**).
  El `809599` (pistola ST-2600 blue) usó la de la 809660 pero **el producto se
  eliminó después a petición del usuario** → quedan **47 productos**.
- **4 productos siguen sin foto** (placeholder elegante): `3409680` (sorting paddle),
  `MS PEROFIXER`, `200075552` (boquilla baja presión, stock 6), `5070 WALL`
  (motobomba, stock 1). Volcados a `docs/PENDIENTES-FOTOS.md` + Excel regenerado.
- **`stock` eliminado del catálogo público** (`847bd31`): sin cantidades ni badges
  en MS Schippers ni Chore-Time. Las 7 marcas ya no mostraban cantidades. La página
  privada de Logística conserva sus piezas (es interna).

### 1.2 Sitio público — mejoras transversales (commits `a8b025a`, `5e5ae4e`, `336215a`)

- **Menú Líneas:** todas las marcas aparecen como disponibles (desktop y móvil);
  desapareció la sección "Próximamente" del menú.
- **Rendimiento:** code-splitting por ruta — el JS inicial bajó de **816 kB a
  356 kB (105 kB gzip)**; cada catálogo se descarga solo al visitarse.
- **WhatsApp flotante** global en el sitio público (botón verde, wa.me/524771284661).
- **MS Schippers migrado al nuevo diseño de marca** (mismo patrón que las 7 marcas:
  hero teal con franja degradada, categorías con conteo, sticky que respeta la
  altura real del header — esto corrige lo que "se veía mal" al deslizar). El modal
  conserva beneficios/aplicaciones/folletos PDF por línea.

### 1.3 Panel — Pedidos abiertos: cómo funciona (verificado, sin cambios de código)

Pipeline completo auditado y sano:
1. Origen: Excel `Y:\1 - CONTROL DE ALMACEN\BASES DE DATOS\BD pedidos pendientes por facturar.xlsx` (hojas PEDIDOS / DETALLE_PEDIDOS), solo lectura.
2. Sync cada 30 min por la tarea de Windows **`3P-Sync-Excel`** → tablas `sync_*` en `data/users.db` (copia local en `data/cache/` para no bloquear el archivo en red). Últimos runs: todos OK.
3. API `GET /api/ventas/pedidos-vivos` lee las tablas sync (no el Excel en cada petición).
4. Frontend (`/dashboard`, pestaña Pedidos): carga al entrar/al tab/al buscar; **sin auto-refresh** (así lo quiere el usuario).

**Totales verificados contra el Excel origen (coinciden exacto):** 34 pedidos vivos,
importe_total **$1,448,005.05 MXN**, facturado $3,670,653.46, saldo $4,151,793.10.
Sin pedidos USD. El total del footer suma lo filtrado (el filtro se aplica en API).
Observación para ventas: el **saldo (columna PENDIENTE_FACTURAR del Excel) es ~3x
mayor que el importe calculado** — confirmar significado de esa columna.

### 1.4 Historial de ventas — cliente vacío en facturas sin pedido (commit `eae2219`)

- **Causa:** filas `FACTURA SIN ORIGEN` en `VENTAS_FACTURACION_BASE.xlsx` no traen
  `Cliente Pedido` (el Excel solo lleva cliente vía pedido). **2,427 de 28,984 filas
  de factura (8.4%)**. Ejemplos: A0010243, A0010257.
- **Fix implementado (opción 2):** el backend rellena el cliente vacío desde el
  espejo SAE (`sae_facturas` + `sae_clientes`, una sola consulta por carga de caché,
  mejor esfuerzo con reintento). Resultado: **0 filas sin cliente**. A0010243 →
  GRUPO PECUARIO SAN ANTONIO; A0010257 → JOAN CARLOS ALVAREZ HERNANDEZ.
- 154 tests del API pasan; backend reiniciado y verificado en producción.
- **Indicación redactada para la sesión de Kimi (proyecto CJ / Excel), opción 1:**
  agregar al final de la hoja `Seguimiento_Documental` las columnas
  **"Clave Cliente Factura"** y **"Nombre Cliente Factura"**, llenas para todas las
  filas de factura desde la cabecera de la factura en SAE (CVE_CLPV + CLIE01), sin
  renombrar columnas existentes. El usuario la pasará en la otra sesión.

---

## 2. Pendiente para mañana

1. **Rediseño de la sección Pedidos abiertos** (la pidió el usuario): definir
   dirección visual (KPIs arriba: total importe / saldo / parciales vs pendientes;
   o tarjetas en vez de tabla; totales destacados). Confirmar con ventas el tema
   saldo vs importe (§1.3).
2. **Pasar la indicación a la sesión de Kimi (CJ)** (texto en §1.4) y dar seguimiento
   a las nuevas columnas del Excel.
3. **Revisar con el usuario** `/marcas/ms-schippers` ya migrada (sticky al deslizar).
4. **Revisar la foto de variante del Q5** (1 L mostrando foto del envase de 20 L) —
   decidir si se queda o se pide la real a almacén.
5. **Decisión TIGSA:** en el menú ya aparece disponible, pero su tarjeta en la
   galería del inicio sigue "Próximamente" porque no tiene productos. Decidir si se
   publica como activa o se espera.
6. **Fotos reales de almacén** para los 4 Schippers sin foto (ver §1.1) al recibirlas.
7. **Volcar a `docs/PENDIENTES-FOTOS.md`** la lista de fotos dudosas de FANCOM (12)
   y LUBING (10) que quedaron en reportes de sesiones anteriores.
8. Revisar `docs/PENDIENTES-FOTOS.xlsx` con almacén (acumulado: Georgia Poultry,
   SBM, LB White, AMT, ALKE, MS Schippers).

---

*Commits de hoy: `d9e45aa` `0114468` `47446aa` `847bd31` `a8b025a` `5e5ae4e`
`336215a` `eae2219` — todos en `master` y desplegados (sitio) / reiniciados (API).*
